# Frontend Environment Variables (for Vercel deployment)
# Copy this file to .env.local for local development

# Backend API URL - will be different for local vs production
REACT_APP_API_URL=http://localhost:5001

# Supabase Configuration (if needed for frontend)
REACT_APP_SUPABASE_URL=your-project-url
REACT_APP_SUPABASE_ANON_KEY=your-anon-key

# Production backend URL (set this in Vercel environment variables)
# REACT_APP_API_URL=https://your-backend-url.onrender.com
