# Backend Environment Variables - LOCAL DEVELOPMENT MODE
# Copy this file to .env for local testing with Plaid sandbox

# Server Configuration
PORT=5001
NODE_ENV=development

# Supabase Configuration (Optional for local testing)
SUPABASE_URL=https://your-project-url.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Zapier Integration (Optional)
ZAPIER_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/your-webhook-url

# Plaid Configuration - SANDBOX MODE (for testing)
# Get sandbox credentials from: https://dashboard.plaid.com/
PLAID_CLIENT_ID=your-plaid-client-id
PLAID_SECRET=your-plaid-sandbox-secret
PLAID_ENV=sandbox

# CORS Configuration - Local development
FRONTEND_URL=http://localhost:3000

# ========================================
# NOTES FOR LOCAL DEVELOPMENT:
# ========================================
# - Use PLAID_ENV=sandbox for testing with fake banks
# - Sandbox test credentials: user_good / pass_good
# - Supabase is optional for local testing
# - Survey data will still be sent to Zapier if configured

