# Backend Environment Variables - PRODUCTION MODE
# Copy this file to .env and add your production credentials

# Server Configuration
PORT=5001
NODE_ENV=production

# Supabase Configuration (REQUIRED)
SUPABASE_URL=https://your-project-url.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Zapier Integration (Optional - for survey email notifications)
ZAPIER_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/your-webhook-url

# Plaid Configuration (REQUIRED for banking features)
# Get credentials from: https://dashboard.plaid.com/
PLAID_CLIENT_ID=your-plaid-client-id
PLAID_SECRET=your-plaid-production-secret
PLAID_ENV=production
# PLAID_ENV options: sandbox (testing), development (testing), production (live)

# CORS Configuration - Production URLs
# Add all your frontend URLs (comma-separated, no spaces)
FRONTEND_URL=https://luni.ca,https://www.luni.ca

# For local development, use:
# FRONTEND_URL=http://localhost:3000